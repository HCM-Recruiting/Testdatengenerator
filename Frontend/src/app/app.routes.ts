import { Routes } from '@angular/router';
import { PropertyWindowComponent } from './property-window/property-window.component';

export const routes: Routes = [
  { path: '', component: PropertyWindowComponent },
];
